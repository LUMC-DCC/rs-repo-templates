# Access and publish the documentation

Read these Markdown files directly in the repository or through a hosting
service that renders Markdown, such as GitHub or GitLab.

To publish a package website, select pkgdown as the documentation builder. The
generated site will include the README, package function reference, and release
news while these narrative Markdown pages remain readable in the repository.
