# Build the package website

Install the packages declared in `DESCRIPTION`, then preview the site:

```bash
@@DOCS_PREVIEW@@
```

Build without opening a browser:

```bash
@@DOCS_BUILD@@
```

The generated website is written to `@@DOCS_OUTPUT@@`. Narrative project documentation
remains under `docs/` so it can also be read directly in the repository.
